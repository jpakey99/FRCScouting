# events = tba.events(2020)
#   for event in events:
#     if event['week'] == 0:
#       matches = tba.event_matches(event['key'])
#       for match in matches:
#         for team in match['alliances']['team_keys']:
#           team_dict[team] = match['alliances']
